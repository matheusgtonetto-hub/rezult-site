{
  "name": "rezult-site",
  "version": "1.0.0",
  "scripts": {
    "dev": "browser-sync start --server --files '*.html, *.css, *.js, build/*.js, logos/*' --port 3000",
    "dev:paineis": "vite build --watch",
    "build": "vite build",
    "vercel-build": "vite build"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "browser-sync": "^3.0.3",
    "vite": "^7.3.6"
  },
  "dependencies": {
    "motion": "^12.42.2",
    "preact": "^10.29.8"
  }
}
